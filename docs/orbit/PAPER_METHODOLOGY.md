# OuRboros: 面向自主编程智能体的自进化认知架构
## 方法 (Methodology) — 论文方法部分 V5.0

> **编制说明**: 基于 `ARCHITECTURE.md`(源码基线)、`PAPER_INTEGRATION_ANALYSIS.md`(融合方案 V9.0)、`METHODOLOGY.pdf`(方法论草稿)、`Ouroboros_SelfEvolution_Paper.pdf` 与 `evolution_innovation_directions.pdf`(创新方向) 系统整合。
> **用途**: 作为论文 Methods 章节的正文。第一部分为模块重组决策摘要,第二部分为论文正文。
> **公式统计**: 共 20 个编号公式(式 1–20),分布于各模块:IRM(4)、SEE(6)、SSE(4)、AMS(3)、GSL(3)。

---

# 第一部分 模块重组决策摘要

## 1.1 三个合并问题的结论

### 问题 1: Unified Smart Router + Harness Tree → 统一为"智能路由模块"?

**结论:可以,且应当统一。** 二者共享同一逻辑前提——"任务在进入执行前必须被决策",仅粒度不同:

| 维度 | Unified Smart Router | Harness Tree | 统一后的角色 |
|------|---------------------|--------------|-------------|
| 决策对象 | 工具 + 技能(资源级) | 提示词 + 上下文 + 预算(配置级) | 配置级承载资源级 |
| 决策粒度 | 单任务一次路由 | 任务 → 分支 → 子任务树 | 树上每个节点做一次路由 |
| 共享依赖 | 任务分类器 | 任务分类器(同一份) | **共享同一个统一任务分类器** |
| 反馈来源 | routing_history.jsonl | 轨迹经验 | 共同消费轨迹反馈 |

统一后命名为 **智能路由模块 (Intelligent Routing Module, IRM)**,内含四个子引擎:统一任务分类器 → 工具-技能联合路由 → Harness Tree 配置适配树 → 执行快照与处置引擎。

### 问题 2: Multi-Agent Evolver + TEL + Prompt Optimization → 统一?

**结论:可以,统一命名为"自进化引擎 (Self-Evolution Engine, SEE)"。** 三者构成三角反馈闭环:

| 子模块 | 回答的问题 | 闭环机能 |
|--------|-----------|---------|
| Trajectory-based Experience Learning (TEL) | 学什么? | **经验源**:将原始轨迹转为带步骤级信用分配的结构化经验 |
| Multi-Agent Evolver (MAE) | 怎么做? | **执行体**:Analyzer→Researcher→Builder→Verifier 四阶段方法论 |
| Prompt Optimization Engine (POE) | 用什么改? | **策略层**:将历史成败模式写回决策提示词 |

三者关系非 pipeline,而是三角反馈:轨迹经验为进化提供适应度信号 → 进化产生新轨迹 → 提示词优化将轨迹洞察转译为指令约束来引导进化。

### 问题 3: 技能单独成模块,记忆统一归并?

**结论:完全合理,构成"双轨进化"架构。**

- **技能自进化引擎 (SSE)**:进化对象为"能力工件"(skill 资产),与 SEE 的"系统实现"进化正交。技能具有独立生命周期、触发机制与伦理约束。
- **自适应记忆基座 (AMS)**:Session Index(检索端) + MemOS(外部化端) + Context Cache(执行端) + Smart Memory(写入端) 共同回答"记忆如何被评估、索引、外化、加载"。

## 1.2 完整模块化体系

| # | 模块 | 缩写 | 源码基线 | 创新来源 | 论文定位 |
|---|------|-----|---------|---------|---------|
| 1 | 智能路由模块 | IRM | 静态工具注册表 + 单一 SYSTEM.md | Unified Smart Router + Harness Tree | 决策前端 |
| 2 | 自进化引擎 | SEE | 二元进化决策 | TEL + MAE + POE | 系统级进化(核心) |
| 3 | 技能自进化引擎 | SSE | 技能加载器 | Hermes-Style Skill Evolution | 能力级进化 |
| 4 | 自适应记忆基座 | AMS | 10 种扁平文件记忆 | Smart Memory + Session Index + MemOS + Context Cache | 认知地基 |
| 5 | 治理与安全层 | GSL | 免疫表面/三方审查/审计 | 进化安全三层保障 | 横向包络 |
| 6 | 多智能体编排与后台意识 | MOC | 蜂群黑板 + 任务树 | 进化并行引擎 | 支撑器官 |

## 1.3 框架图构思

模块关系为 **循环 + 辐射 + 包络** 三类拓扑的叠加:

- **循环关系**(核心环): Execute → Reflect → Optimize → Store → Execute
- **辐射关系**(四柱环绕): IRM / SEE / SSE / AMS 环绕核心环,彼此间为双向数据流与反馈流
- **包络关系**(治理环): GSL 横向贯穿所有自修改动作,MOC 提供并行执行骨架

---

# 第二部分 方法正文

## 3.1 设计哲学

传统编程智能体运行于开环范式:每次任务调用从零开始,丢弃先前交互中积累的上下文知识。这一架构局限表现为三种病理:(i) 上下文失忆——无法利用跨会话经验;(ii) 路由刚性——静态调度无法适应任务异质性;(iii) 进化停滞——缺乏自我改进机制。

OuRboros 通过统一的自进化认知架构应对上述局限,其设计灵感来源于衔尾蛇(Ouroboros)符号——象征执行、反思与更新的永续循环。系统将每次交互视为同时充当执行输出、训练信号与进化基质的轨迹。四项设计原则统摄整个架构:

**原则 1: 循环而非流水线。** 模块围绕中心认知循环组织;每个模块既消费又产出循环的轨迹数据。

**原则 2: 双轨进化。** 系统沿两条正交基质进化:*系统实现*(配置、提示词、源码)通过自进化引擎 SEE;*能力工件*(技能)通过技能自进化引擎 SSE。

**原则 3: 治理先于能力。** 每个自修改动作被治理包络(免疫表面、跨厂商评审、审计账本、预算免疫)所包裹,使自修改的能力与对该能力的约束成为同一架构的两面。

**原则 4: 增量嵌入。** 所有增强以钩子式、最小差异扩展实现(12 个新模块,约 4,400 行代码,对核心循环语义零改动),保持系统已验证的安全性和连续性属性。

## 3.2 符号与问题设定

本文使用的核心符号汇总如表 1 所示。

**表 1. 核心符号表**

| 符号 | 含义 |
|------|------|
| $\tau = (s_0, a_0, o_0, \ldots, s_T)$ | 一条执行轨迹,包含状态、动作、观测序列 |
| $c(s_i) \in [0,1]$ | 轨迹中步骤 $s_i$ 的信用值 |
| $u(T)$ | 轨迹 $T$ 的综合效用 |
| $\mathcal{H} = (V, E)$ | Harness Tree,任务分解的有向无环图 |
| $B(v)$ | 节点 $v$ 的资源预算 |
| $S = (\text{name}, \text{trigger}, \text{fitness}, \ldots)$ | 技能,强类型能力组件 |
| $\text{fitness}(S)$ | 技能 $S$ 的适应度 |
| $\text{importance}(b)$ | 记忆块 $b$ 的重要性 |
| $\Phi$ | 认知循环算子 |
| $V(\cdot)$ | 系统综合性能评估函数 |
| $\mathcal{G}$ | 治理约束集合 |
| $\text{attr}(p_i)$ | 提示词段 $p_i$ 的反事实归因 |

系统状态在时刻 $t$ 表示为 $s_t = (C_t, M_t, K_t, \Pi_t, B_t)$,分别对应当前上下文、多层记忆状态、技能库、提示词配置和资源预算。所有模块围绕认知循环 $\Phi$ 组织,每个模块既消费 $\Phi$ 产生的轨迹数据,又向 $\Phi$ 反馈改进信号。

## 3.3 系统架构总览

[图 1: 系统架构总览]

OuRboros 架构由六个主要模块组成,围绕中心认知循环组织。拓扑结构刻意设计为**非顺序的**:模块间通过三种显式连接类型交互——**数据流**(运行时交换的产物)、**反馈流**(返回至生产者的学习信号)、**控制依赖**(对动作施加的治理约束)。认知循环(核心环)被四个柱模块环绕——智能路由模块(输入决策)、自进化引擎(系统级进化)、技能自进化引擎(能力级进化)、自适应记忆基座(知识基础)——而治理与安全层和多智能体编排层构成水平包络,约束并并行化所有自修改活动。

```
┌─────────────────────────────────────────────────────────────────────────────┐
│             治理与安全层  (GSL, 横向包络 · 控制依赖)                           │
│   免疫表面 protected paths │ 跨厂商三方评审 triad │ 审计账本 audit ledger      │
│   预算免疫 budget immunity │ 唯一提交者 sole committer │ 回滚 rollback         │
└───────┬───────────────────────────┬───────────────────────────┬─────────────┘
        │ ┄┄┄┄ 约束一切自修改动作 ┄┄┄┄│┄┄┄┄ 约束一切自修改动作 ┄┄┄┄│
┌───────▼───────────────────────────▼───────────────────────────▼─────────────┐
│                        多智能体编排与后台意识  (MOC)                           │
│   蜂群黑板 (tree_note/tree_read) │ 任务树 │ 后台意识(主动进化触发器)          │
└───────┬─────────────────────────────────────────────────────────────────────┘
        │ 任务分配 / 并行执行骨架
┌───────▼─────────────────────────────────────────────────────────────────────┐
│   ┌─────────────────────────┐         ┌──────────────────────────────────┐  │
│   │  IRM 智能路由模块         │ ──▶▶▶  │  认知循环核心 Cognitive Cycle     │  │
│   │  · 统一任务分类器          │ 执行轨迹 │  Execute → Reflect → Optimize   │  │
│   │  · 工具-技能联合路由       │         │  → Store → (循环)               │  │
│   │  · Harness Tree 配置树    │ ◀─◀─◀   │                                │  │
│   │  · 执行快照与处置          │ 路由反馈 │   (唯一自进化主循环)             │  │
│   └───────────┬─────────────┘         └─────┬──────────────┬────────────┘  │
│               │ 路由决策                     │ 轨迹/反思      │ 经验/策略     │
│               ▼                             ▼              ▼               │
│   ┌──────────────────────┐  ┌────────────────────────┐  ┌────────────────┐ │
│   │  SEE 自进化引擎        │  │  SSE 技能自进化引擎      │  │ AMS 记忆基座    │ │
│   │  · 轨迹经验学习(TEL)   │  │  · 技能自动生成          │  │ · SmartMemory  │ │
│   │  · 多智能体进化器(MAE) │  │  · Nudge 引擎           │  │ · SessionIndex │ │
│   │  · 提示词优化(POE)     │  │  · GEPA 技能进化        │  │ · MemOS        │ │
│   │  系统级进化             │  │  · 质量感知路由(↔IRM)   │  │ · ContextCache │ │
│   └──────────────────────┘  └────────────────────────┘  └────────────────┘ │
└─────────────────────────────────────────────────────────────────────────────┘
   图例: ──▶ 数据流   ─◀─▶ 反馈流   ┄┄▶ 控制依赖
```

**图 1.** OuRboros 架构总览。认知循环构成核心;IRM、SEE、SSE、AMS 为柱模块,与核心双向交互;GSL 与 MOC 构成水平包络。

## 3.4 智能路由模块 (IRM)

### 3.4.1 问题建模

真实编程任务呈现极端异质性:单次会话可能涉及简单拼写修正(延迟敏感、最小上下文)、复杂多文件重构(上下文密集、可并行化)、以及微妙的并发缺陷诊断(推理密集、顺序执行)。基线系统对所有任务无差别地暴露全部 35 个工具和所有技能,每次请求浪费 7,000–17,500 个 token 于工具模式,并产生约 20% 的工具选择错误率。

我们将调度问题建模为**带资源约束的上下文赌博机**。在每个决策时刻 $t$,系统观测任务特征向量 $x_t$(复杂度估计、所需工具、依赖深度、历史成功率),并选择调度动作 $a_t$,联合决定:(i) 执行通道(轻量委派 vs. 完整认知循环);(ii) 智能体配置(模型、工具集、上下文预算、提示词分支);(iii) 隔离级别(共享状态 vs. 私有快照)。路由决策最小化期望风险:

$$a_t^* = \arg\min_{a \in \mathcal{A}} \;\mathbb{E}\left[C(a, x_t)\right] + \lambda \cdot \text{Var}\left[C(a, x_t)\right] \tag{1}$$

其中 $C(a, x_t)$ 为成本函数,$\lambda \geq 0$ 为风险厌恶系数,$\mathcal{A}$ 为可行调度动作空间。路由决策记录为元组 $(x_t, a_t, \text{outcome}_t)$,反馈至自进化引擎以改进未来决策——闭合调度-记忆反馈环。

### 3.4.2 统一任务分类器

单一轻量级分类器服务于所有下游组件(工具路由、技能路由、Harness 选择),保证每个任务仅分类一次。分类器采用两层架构:

- **规则层**:确定性信号(工作区存在性 $\rightarrow$ 编码;任务类型标签 $\rightarrow$ 研究/知识/简单)以零 LLM 成本提供即时分类。
- **LLM 层**:当规则层不确定时,调用紧凑 LLM 将任务分类为 $K$ 个意图类别之一。

单分类器设计是统一调度一致性的关键:工具路由、技能路由和 Harness 选择消费同一标签,误分类在一处而非三处被纠正。

### 3.4.3 统一工具-技能路由

路由消费任务标签,联合输出过滤后的工具集和排序后的技能集。

**工具路由**:每个意图类别映射到一个策划好的工具集(10–15 个工具 vs. 基线 35 个),将模式 token 减少 50–70%。

**技能路由**:每个技能获得相关性评分,联合考虑标签重叠、名称匹配、描述匹配,以及由技能自进化引擎提供的**质量项**:

$$\text{score}(s_i) = \underbrace{\alpha \cdot \text{relevance}(s_i, q)}_{\text{任务相关性}} + \underbrace{\beta \cdot \text{quality}(s_i)}_{\text{进化质量}} \tag{2}$$

其中质量项 $\text{quality}(s_i)$ 综合自编写加成、高成功率加成和版本加成。选取评分超过阈值的前 $K$ 个技能注入提示词。

### 3.4.4 Harness Tree 调度引擎

Harness Tree 将路由从单个任务扩展到执行树——表示任务分解的层次化配置 $\mathcal{H} = (V, E)$。每个节点 $v \in V$ 对应一个子任务,每条边表示依赖关系。每个分支捆绑任务特定的系统提示、工具集和记忆配置。

**自适应预算分配**。每个分支携带与估计复杂度成正比的资源预算:

$$B(v) = B_{\text{total}} \cdot \frac{c(v)^{\eta}}{\sum_{v' \in \text{Siblings}(v)} c(v')^{\eta}} \tag{6}$$

其中 $c(v)$ 为节点 $v$ 的复杂度估计,$\eta > 0$ 为温度参数,控制预算分配的集中程度。

**级联上下文继承**:子节点继承父节点上下文的压缩摘要,避免每个委派层级的冷启动:

$$C(v_{\text{child}}) = C_{\text{base}}(v_{\text{child}}) \oplus \text{Compress}(C(v_{\text{parent}})) \tag{7}$$

其中 $\oplus$ 表示上下文拼接,$\text{Compress}(\cdot)$ 为上下文压缩函数。

**提前终止剪枝**:若父节点的执行结果使子节点变得不必要,则剪除剩余子树并回收资源。

**算法 1** Harness Tree 动态调度

```
输入: 初始任务 q, 资源预算 B_total
输出: 执行结果 r

1:  H ← BuildTree(q)
2:  while V_active(H) ≠ ∅ do
3:      v ← SelectNextNode(H)
4:      (tools, skills, prompt) ← Router(classify(v))
5:      result ← Execute(v, tools, skills, prompt)
6:      UpdateBudget(B, cost(result))
7:      if result.success then
8:          PropagateSuccess(v, H)
9:      else
10:         H ← Reconfigure(H, result.error)
11:     end if
12:     StoreToMemory(result, v)
13: end while
14: return AggregateResults(H)
```

### 3.4.5 执行快照与处置引擎

为保持委派子智能体执行的隔离性和可审计性,系统创建当前执行状态的私有快照;子智能体在快照内操作,产出结果和置信度;处置引擎根据正确性、一致性和效率标准评估输出,发出 `accept`(合并至父上下文)、`revise`(用修改后的指令重新调度)或 `escalate`(提升至更高执行层)之一。处置被记录至轨迹,使系统能够学习哪些委派模式成功。

> **贡献 1 (统一调度)**: 将统一智能路由器、Harness Tree 和执行快照整合为单一调度框架,在多个粒度上适应任务复杂度——一种带执行结果闭环反馈的上下文赌博机路由公式。

## 3.5 自进化引擎 (SEE)

### 3.5.1 认知循环:统一逻辑

SEE 实现赋予系统名称的自指循环。引擎不是三个独立组件,而是具有三个协调子系统的单一循环:

$$\Phi = \sigma_{\text{store}} \circ \sigma_{\text{optimize}} \circ \sigma_{\text{reflect}} \circ \sigma_{\text{execute}} \tag{3}$$

其中 $\sigma_{\text{execute}}$ 执行任务并记录轨迹片段 $\tau_i = (s_i, a_i, o_i, r_i)$;$\sigma_{\text{reflect}}$ 通过轨迹经验学习(TEL)将原始轨迹转化为结构化、信用评分的经验;$\sigma_{\text{optimize}}$ 通过提示词优化引擎(POE)精炼系统指令,并通过多智能体进化器(MAE)执行结构化改进循环;$\sigma_{\text{store}}$ 将所有产物持久化至自适应记忆基座。

### 3.5.2 轨迹经验学习 (TEL)

并非所有轨迹等值。每条轨迹 $T$ 获得综合效用评分:

$$u(T) = \omega_1 \cdot u_{\text{outcome}} + \omega_2 \cdot u_{\text{efficiency}} + \omega_3 \cdot u_{\text{generalizability}} + \omega_4 \cdot u_{\text{novelty}} \tag{8}$$

其中 $\sum_i \omega_i = 1$,各分量分别为:结果质量(成功/部分成功/失败)、效率(归一化 token 消耗的倒数)、泛化性(轨迹在其他任务上的可复用度)、新颖性(与已有轨迹集合的最小距离)。

TEL 实现四阶段优化流水线:去重(结构相同的轨迹合并)、压缩(移除冗余中间状态)、标注(反思提取的模式)、评分(综合效用)。

**核心创新:步骤级信用分配。** 基线系统仅记录进化周期的聚合结果(吸收/放弃/无操作)。TEL 为每个步骤分配归一化信用:

$$c(s_i) = \text{clip}\Big(0.5 + \Delta_{\text{success}} + \Delta_{\text{error}} + \Delta_{\text{speed}} + \Delta_{\text{token}},\; 0,\; 1\Big) \tag{4}$$

其中各增量为:$+0.2$(步骤成功)、$-0.3$(步骤出错)、$+0.1$(快速完成)、$+0.1$(token 高效)。信用分布识别**关键步骤**——信用最高的前 3 步和最低的 2 步——产出:(i) 成功因素(什么有效);(ii) 失败因素(什么失败);(iii) 推荐工具(从相似过去目标的关键步骤模式中聚合)。

信用信号可进一步被四类反馈丰富:隐式行为信号(技能复用频率、编辑模式)、显式交互信号(点赞、纠正、跳过)、效率信号(token 成本、延迟)、对比信号(新旧配置在同一任务上的表现)。

在新任务上,混合检索(语义相似度 + 结构匹配 + 结果过滤)获取前 $k$ 个相关经验作为少样本示例和进化的适应度基线。

### 3.5.3 多智能体进化器 (MAE)

基线进化循环是二元决策:固定提示词决定是否推进改进周期;执行是单智能体、无结构的(历史吸收率约 40%)。MAE 将执行阶段替换为**结构化四阶段方法论**,每个阶段委托给专门角色:

**Analyzer(分析器)** 对任务轨迹和改进积压执行失败分析,提取根因并排列优先改进。优先级计算综合考虑影响度、频率和修复成本:

$$\text{priority}_k = \frac{\text{impact}_k \cdot \text{frequency}_k}{\text{cost}_k + \epsilon} \tag{9}$$

其中 $\text{impact}_k$ 为估计影响,$\text{frequency}_k$ 为出现频率,$\text{cost}_k$ 为修复成本估计。

**算法 2** 多智能体进化流水线

```
输入: 任务轨迹 τ, 改进积压 backlog, 代码库上下文 C
输出: 验证后的变更 patch

1:  (root_causes, priorities) ← Analyzer.analyze(τ, backlog)
2:  for each root_cause in priorities do
3:      (plan, risks) ← Researcher.design(root_cause, C)
4:      patch ← Builder.implement(plan)
5:      verdict ← Verifier.evaluate(patch, objective)
6:      if verdict = merge then
7:          Commit(patch)
8:      else if verdict = revise then
9:          goto line 3 with feedback
10:     else
11:         Reject(patch)
12:     end if
13: end for
```

MAE 与基线的治理化自修改管线(观察 → 规划 → 修改 → 评审 → 提交 → 重启)自然对接:四个角色占据修改阶段,而现有的跨厂商三方评审、范围评审、过期绑定、唯一提交者和回滚机制作为硬门控保持不变。这是刻意设计——进化能力与进化约束必须同步扩展。

### 3.5.4 提示词优化引擎 (POE)

系统将自身指令视为可优化参数。给定当前提示词 $P$ 和轨迹结果质量 $q$,POE 执行四步优化:

1. **分解**:将提示词分解为 $m$ 个语义段(工具使用指南、错误处理协议、输出格式规范等)。

2. **归因**:每个段的贡献通过反事实估计:

$$\text{attr}(p_i) \approx \mathbb{E}[q(T) \mid P] - \mathbb{E}[q(T) \mid P \setminus \{p_i\}] \tag{10}$$

即"如果段 $p_i$ 缺失,结果会有何不同?"——通过与不同提示词配置下相似过去轨迹的比较来近似。

3. **生成**:通过模式重写、反思驱动修订和进化变异生成候选修改。

4. **选择**:使用代理指标(从相似过去执行中估计的结果质量)评估并选取最优候选:

$$p_i^* = \arg\max_{\text{Candidate}_j} \hat{q}(\text{Candidate}_j) \tag{11}$$

提示词变体经历保守的生命周期管理:候选 → 试用期($n$ 个会话中测试)→ 晋升(跨多样任务验证)→ 退役(分布漂移导致性能退化),确保优化是自适应的但从不鲁莽。

### 3.5.5 子系统协调:三角反馈而非流水线

TEL、MAE 和 POE 构成闭合三角而非链式:TEL 为 MAE 提供适应度信号(无轨迹,进化将盲目),为 POE 提供归因数据;MAE 产出策略变体,生成新轨迹(无进化,轨迹将停滞);POE 在二者之间中介,将轨迹洞察转译为引导进化的指令精炼。

> **贡献 2 (经验驱动的自进化)**: 统一的自进化引擎,其中步骤级信用分配的轨迹学习、四角色结构化多智能体进化方法论、和基于归因的提示词优化构成单一自指循环——用持续改进、可验证的进化流水线替代二元进化决策。

## 3.6 技能自进化引擎 (SSE)

### 3.6.1 动机:第二条正交进化轨道

技能是经验的最成熟形式——跨会话测试和精炼的可复用能力单元。在基线中,技能是静态资产:从磁盘发现、按需执行、永不自动创建或改进。SSE 通过使技能本身成为进化基质来弥合这一差距。

SSE 与 SEE 保持独立,因为二者进化不同基质:SEE 进化*系统如何运行*(实现);SSE 进化*系统能做什么*(能力)。技能进化还携带独立的生命周期(Seed → Draft → Active → Mature → Archived)、触发机制和伦理约束(社区技能永不修改——仅自编写技能可进化)。

### 3.6.2 从轨迹自动生成

**定义 1 (技能蒸馏条件).** 当三个条件同时满足时,从任务轨迹中蒸馏技能:

$$\text{Distill}(T) \iff |\text{tool\_calls}(T)| > 5 \;\wedge\; \exists\, \text{self\_repair}(T) \;\wedge\; \text{success}(T) \tag{12}$$

即:(i) 超过 5 次工具调用;(ii) 智能体执行了自修复(错误后成功重试);(iii) 任务成功。LLM 从提取的关键步骤合成技能(name, description, parameters, executable scripts, tags);产物存储于 `skills/self/` 下,带有来源标记,并记录在生成历史中。

### 3.6.3 Nudge 引擎

Nudge 引擎以固定周期(每小时)提示智能体审视近期工作,检测:(a) 值得蒸馏为新技能的可复用模式;(b) 失败的 `skill_exec` 调用,指示现有技能需要进化。Nudge 被记录,仅在分析支持时才具体化为生成/进化动作。

### 3.6.4 基于 GEPA 的技能进化

**定义 2 (进化资格).** 满足以下条件的技能进入进化流程:

$$\text{Eligible}(S) \iff \text{source}(S) = \text{self} \;\wedge\; \text{exec\_count}(S) \geq 10 \;\wedge\; \text{sr}(S) < 0.8 \tag{13}$$

即:自编写、执行次数 $\geq 10$、成功率 $< 0.8$。进化采用遗传-Pareto 提示词优化(GEPA):

1. **失败分析**:聚合常见错误消息,LLM 生成改进建议。
2. **种群构建**:构建变体集,包括原始技能、建议引导的变异和随机变异(添加错误处理/改进描述/优化参数)。
3. **评估**:对每个变体的适应度进行评分:

$$\text{fitness}(S') = \frac{1}{N} \sum_{i=1}^{N} \mathbb{1}[\text{success}(S', \text{task}_i)] \tag{5}$$

4. **选择**:仅当最佳变体严格超过当前版本的适应度时才推进:

$$S_{\text{new}} = \begin{cases} \arg\max_{S' \in \mathcal{P}} \text{fitness}(S') & \text{if } \max_{S'} \text{fitness}(S') > \text{fitness}(S_{\text{current}}) + \delta \\ S_{\text{current}} & \text{otherwise} \end{cases} \tag{14}$$

其中 $\delta > 0$ 为最小改进阈值,防止微小波动触发无意义的版本递增。

所有进化事件追加至 `skill_evolution_history.jsonl`,使每个技能变更可审计和可逆(当前版本永不被销毁)。

### 3.6.5 质量感知路由接口

进化后的技能反馈至 IRM,路由评分获得质量加权(见式 2),闭合能力循环——更好的技能更频繁地被展示,获得更多执行,接收更快的适应度更新。

> **贡献 3 (能力级自进化)**: 正交的技能进化轨道,具有轨迹驱动的自动生成、基于 Nudge 的触发机制、基于 GEPA 的变体进化与严格适应度门控、以及来源约束(仅自编写技能进化),通过质量感知评分与路由集成。

## 3.7 自适应记忆基座 (AMS)

### 3.7.1 基线与差距

基线已维护十种记忆类型(工作暂存板、身份、世界画像、对话块、知识、模式、反思、检查点、积压、注册表),具有代际感知合并和原子并发写入。然而呈现五个弱点:(i) 扁平 FIFO 淘汰,无重要性评估;(ii) 无语义搜索;(iii) 轮换聊天日志归档后实质丢失;(iv) 每任务上下文重建使提示词缓存失效;(v) 完全没有会话概念。AMS 通过四个紧密集成的组件应对这些弱点。

### 3.7.2 Smart Memory:基于重要性的管理(写入端)

暂存板的 FIFO 淘汰被替换为**混合重要性模型**。每个记忆块 $b$ 的重要性评分为:

$$\text{importance}(b) = \alpha \cdot \text{priority}(\text{source}(b)) + \beta \cdot \text{keyword\_score}(b) + \gamma \cdot \text{recency}(b) \tag{15}$$

其中 $\text{priority}$ 为来源先验(错误 > 反思 > 常规),$\text{keyword\_score}$ 为高价值关键词匹配度,$\text{recency}$ 为时间衰减因子。当规则层不确定时,LLM 兜底评分。

淘汰策略移除最低重要性块:

$$b_{\text{evict}} = \arg\min_{b \in \text{Scratchpad}} \text{importance}(b) \tag{16}$$

检索支持标签过滤和重要性阈值查询:

$$\text{Retrieve}(\text{query}, \theta) = \{b \mid \text{sim}(b, \text{query}) > \theta \wedge \text{importance}(b) > \theta_{\text{imp}}\}$$

将重要记忆保留率从约 60% 提升至估计的约 90–95%。

### 3.7.3 Session Index:可搜索历史(检索端)

持久化 SQLite FTS5 索引使整个会话历史可搜索,无需更改任何现有文件:聊天摘要、任务反思和进化检查点增量同步(检查点通过惰性增量同步)。冷调用 `session_search` 工具在查询时暴露全文检索,使历史经验——"我之前如何解决这个问题?"——成为一等资源。索引设计为覆盖层:现有的轮换、归档和写入路径不受影响。

### 3.7.4 MemOS:外部语义记忆(外部化端)

可选的外部记忆提供者添加语义检索和跨项目知识共享。基于钩子设计:每次反思后,反思内容同步至 MemOS 作为文本记忆(带来源标记);上下文构建期间,预取获取前 5 个语义相似记忆并注入为上下文提示。当 MemOS 不可用时,系统优雅降级至基线行为。

**记忆生命周期状态转换**。记忆经历生命周期状态转换,由访问频率、新鲜度和相关性 govern:

$$\text{ephemeral} \xrightarrow{f > \theta_1} \text{working} \xrightarrow{\text{stale} > \theta_2} \text{archived} \xrightarrow{f < \theta_3} \text{forgotten} \tag{17}$$

其中 $f$ 为访问频率。相似记忆周期性合并:

$$m_{\text{merged}} = \text{Merge}(\{m_i \mid \text{sim}(m_i, m_j) > \theta_{\text{merge}}\})$$

新经验与存储记忆之间的冲突通过保留两者并附加上下文标签来解决。

### 3.7.5 Context Cache 与资源生命周期管理器(执行端)

三级缓存将记忆基座与有限上下文窗口对齐:L1 会话缓存(当前会话,LRU + 相关性加权)、L2 工作集(跨会话活跃项目,LFU + 新鲜度衰减)、L3 归档(无界,磁盘支撑,永不淘汰)。稳定上下文块(身份、世界画像、知识索引)在会话内复用,将提示词缓存命中率从约 0% 提升至估计的 60–80%。资源生命周期管理器执行比例预算分配、预算压力下的优雅降级、任务完成后的资源回收,以及**基于快照的垃圾回收**——执行快照被视为不可变对象,在无引用时回收,保持内存增长相对于会话数的亚线性。

> **贡献 4 (认知连续性)**: 四面记忆基座——重要性管理写入、全文可搜索历史、外部语义记忆、缓存感知上下文加载——消除失忆病理,同时保持已验证的基线记忆语义(原子性、代际感知)完好。

## 3.8 治理与安全层 (GSL)

### 3.8.1 动机

自进化不安全,除非其失败模式被结构性地约束。基线已体现治理架构:受保护路径(免疫表面)、跨厂商三方评审与范围评审、过期绑定(编辑使旧评审失效)、尝试上限、审计账本、预算免疫、唯一提交者、以及自动救援快照。GSL 将这些提升为三层进化安全保障。

### 3.8.2 三层安全保障

**第一层:预测感知修补。** 每次修改携带自述预测("此变更预计将编码任务通过率提高 5%,对研究任务无影响")。后验证比较计算预测准确度作为元指标:

$$\text{Accuracy}_{\text{pred}} = 1 - \frac{|q_{\text{predicted}} - q_{\text{actual}}|}{q_{\text{actual}} + \epsilon} \tag{18}$$

惩罚不准确的进化,使系统学会做出更保守、更可靠的预测。

**第二层:回归保留池。** 保留任务池(按任务类型抽样,加上最难的 10%)在每次进化前后评估。变更接受条件为:

$$\text{Accept} \iff q_{\text{after}} - q_{\text{before}} \geq -\delta_{\max} \;\wedge\; p\text{-value} > 0.05 \tag{19}$$

其中 $\delta_{\max} = 0.05$ 为最大允许退化阈值,统计检验确保退化不显著。

**第三层:跨分支冲突检测。** 当修改触及共享组件(通用提示词、共享技能)时,验证在所有受影响分支上强制执行,而非仅当前任务类型:

$$\text{Validate}(\text{patch}) \implies \forall\, b \in \text{AffectedBranches}: \text{Test}(b, \text{patch}) \tag{20}$$

### 3.8.3 七类失败模式缓解

结合基线的七类失败模式缓解(退化循环、自确认、过期评审、无限重试、预算燃烧、身份漂移、验证偏差),安全论证变为**结构性的**而非行为性的:失败被约束为局部的(隔离工作树)、可逆的(救援快照 + 撤销提交)、可见的(完整审计追踪)、昂贵的(物理尝试账本)——且自修改的最终权威永远不驻留于被修改对象自身。

**命题 1 (治理安全性).** 设 $\mathcal{G}$ 为治理约束集合,由免疫表面完整性、三方评审一致性、预算约束和回归安全四个条件定义。对所有进化操作 $e$,若 $e$ 满足 $\mathcal{G}$ 的全部条件,则系统状态始终保持在安全包络内:

$$\forall\, t, \quad s_t \in \mathcal{G}(s_0)$$

**证明**. 由四个条件的合取,每次进化操作均在 $\mathcal{G}$ 定义的合法状态空间内。由数学归纳法,若 $s_0 \in \mathcal{G}(s_0)$ 且 $e(s_t) \in \mathcal{G}(s_t)$,则 $s_{t+1} = e(s_t) \in \mathcal{G}(s_0)$。$\blacksquare$

> **贡献 5 (治理化自修改)**: 三层进化安全保障——预测感知修补、回归保留验证、跨分支冲突检测——叠加于免疫表面、跨厂商评审、审计账本、预算免疫和唯一提交者执行之上。

## 3.9 跨模块协同:统一视图

六个模块通过三种涌现协同模式形成统一认知架构:

**协同 1:调度-记忆反馈环。** IRM 路由决策被记录至 AMS;Session Index 为相似任务在不同调度配置下提供先验成功率;技能适应度告知路由权重;资源可用性信号防止过度承诺。每个路由决策生成改善未来路由决策的数据。

**协同 2:轨迹-进化协同适应。** TEL 为 MAE 提供适应度信号;MAE 产出策略变体生成新轨迹;POE 通过转译轨迹洞察为指令精炼来中介。系统同时优化其知道什么(轨迹)、如何知道(提示词)、如何行动(策略)。

**协同 3:技能-记忆巩固循环。** 原始轨迹累积于 Session Index → TEL 提取技能种子 → SSE 验证和精炼为成熟技能 → 质量感知路由将其注入执行 → 改进的执行生成更高质量轨迹。这是知识蒸馏流水线,持续将原始经验压缩为可复用专业知识。

## 3.10 理论分析

**命题 2 (经验收敛).** 在每个评估窗口内任务分布平稳且轨迹效用 $u$ 满足 Lipschitz 连续的条件下,当优化步长满足 Robbins-Monro 条件($\sum \alpha_t = \infty$, $\sum \alpha_t^2 < \infty$)时,期望轨迹效用随循环次数 $T \to \infty$ 收敛至局部最优。

**证明概要**. 每个优化步骤(提示词优化、策略进化、技能精炼)是效用曲面上的随机梯度步骤;认知循环协调步骤以防止振荡;AMS 提供减少方差的热启动。由标准随机逼近理论,收敛性得证。$\blacksquare$

**复杂度**. 每循环开销:轨迹检索 $O(\log N)$(近似最近邻);提示词优化 $O(m \cdot k)$(段数 $\times$ 候选数);进化 $O(P \cdot G)$(种群大小 $\times$ 代数)。空间增长由于合并和基于快照的垃圾回收而相对于会话数亚线性。

## 3.11 实现映射:基线上的最小差异增强

所有创新以钩子式桥接的附加模块实现,保持基线核心循环语义。新文件(约 4,400 行):

| 模块 | 文件 | 行数 |
|------|------|------|
| IRM | `smart_router.py` | ~500 |
| IRM | `harness_tree.py` + `harness_configs/` | ~500 |
| SEE | `evolution/trajectory_experience_learner.py` | ~500 |
| SEE | `evolution/multi_agent_evolver.py` | ~400 |
| SEE | `evolution/prompt_optimizer.py` | ~300 |
| SSE | `skill_auto_generation.py` + `skill_evolution.py` + `skill_nudge_engine.py` | ~1,200 |
| AMS | `memory_ext/smart_memory.py` | ~400 |
| AMS | `memory_ext/session_index.py` + `tools/session_search.py` | ~240 |
| AMS | `memory_ext/memos_provider.py` | ~300 |

桥接修改(~100 行)仅在 `agent.py`、`context.py`、`reflection.py`、`agent_task_pipeline.py` 和 `post_task_evolution.py` 的现有生命周期钩子处触碰——如上下文构建、后反思处理、和进化推进决策点。

## 3.12 总结

OuRboros 代表了从静态、无状态编程智能体到动态、自进化认知系统的范式转变。六个模块——智能路由模块、自进化引擎、技能自进化引擎、自适应记忆基座、治理与安全层、多智能体编排——形成统一反馈架构,其中每次执行同时解决当前任务并改进系统的未来能力。五项核心创新:

**(1) 统一调度** (§3.4): 将路由从单点决策升级为贯穿任务树的决策骨架;
**(2) 经验驱动的自进化** (§3.5): 三角反馈闭环替代二元进化决策;
**(3) 能力级自进化** (§3.6): 正交的技能进化轨道,与系统进化构成双轨架构;
**(4) 认知连续性** (§3.7): 四面记忆基座消除失忆病理;
**(5) 治理化自修改** (§3.8): 结构性安全论证使自进化可信。

这些创新共同实现了一种机器自主性形式:系统自主改进自身能力、适应变化环境、并在其整个运行生命周期中积累专业知识。
