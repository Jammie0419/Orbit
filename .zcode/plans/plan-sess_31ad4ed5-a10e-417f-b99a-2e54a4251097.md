## 计划：在 ARCHITECTURE.md 末尾追加"第19节：复杂任务全链路功能调度"

### 目标
在 `ARCHITECTURE.md` 文档末尾（现有第18节之后、页脚之前）追加一个全新的 **第19节**，以一个贯穿多轮用户对话的复杂任务为线索，完整展现 Ouroboros agent 从消息入站到记忆沉淀的全部功能调度链路。

### 修改文件
- `E:\lzm\2026\research\ouroboros\ARCHITECTURE.md` — 在现有内容末尾追加约 800 行新内容

### 新增内容结构

**§19.0 任务场景设定** — 设定一个涉及重构+测试+调研的复杂任务，触发所有子系统

**§19.1 阶段一：消息入站 → 任务创建** — 从浏览器 WebSocket 到 `ws_endpoint()` → `handle_chat_direct()` 的完整入站链路

**§19.2 阶段二：Agent 编排层 — handle_task()** — `OuroborosAgent.handle_task()` 的全部步骤：配置热重载、UsageScope、`_handle_task_scoped()` 分支

**§19.3 阶段三：上下文构建 — _prepare_task_context()** — 16 步详细流程：调度解析 → ToolContext → 智能路由 → Q1A预检 → `build_llm_messages()` 的7个上下文分区

**§19.4 阶段四：LLM 主循环 — run_llm_loop()** — 逐轮展示：
  - Round 1: 初始分析（read_file, search_code）
  - Round 3: 代码写入
  - Round 5: 子代理委派（delegate_start → OAuth2调研）
  - Round 6: 用户追加消息注入（inject_message → _drain_incoming_messages）
  - Round 10: 三方提交审查（commit_reviewed）
  - Round 12: 知识库写入
  - Round 15: 周期检查点注入
  - Round 16: 最终答案

**§19.4A/B/C 子流程展开** — 子代理委派、三方审查、知识库写入的详细调用链

**§19.5 阶段五：终结决策 — _no_tool_final_answer()** — 12步终结流程 + 接受审查面板详解

**§19.6 阶段六：后任务处理管道 — emit_task_results()** — 11步：结果推导 → send_message → task_eval → task_done → 根任务后处理

**§19.7 阶段七：后任务认知管道** — 对话整合、便签整合、任务总结、反思、改进积压、进化晋升

**§19.8 阶段八：Supervisor 事件处理** — 8 种关键事件的完整处理链

**§19.9 阶段九：Worker 分配与进程模型** — assign_tasks() + worker_main() + 崩溃恢复

**§19.10 阶段十：Delivery Candidate 完整生命周期** — 创建→失效→重建→接受

**§19.11 阶段十一：后台意识** — 独立于主任务的意识循环

**§19.12 全链路数据流总图** — ASCII 总览图

**§19.13 完整功能调用清单** — 40 项功能的汇总表（功能名 + 触发时机 + 关键函数）

### 不修改的内容
- 现有 1-18 节保持原样
- 目录（目录中不新增第19节条目，因为这是追加的补充材料）
- 页脚"文档生成方式"保持原位