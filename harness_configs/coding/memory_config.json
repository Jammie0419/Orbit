{
  "description": "Coding branch memory injection: scratchpad (working set) first, project context (identity/world) next, dialogue last.",
  "priority": ["scratchpad", "identity", "environment profile", "dialogue"],
  "exclude": [],
  "max_sections": 4
}