{
  "description": "Research branch memory injection: working set (scratchpad/dialogue) first; identity is low value while researching.",
  "priority": ["scratchpad", "dialogue", "environment profile"],
  "exclude": ["identity"],
  "max_sections": 3
}