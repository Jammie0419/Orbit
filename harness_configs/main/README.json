{
  "description": "Default fallback branch (PAPER 不足 3 harness tree). Empty adjustments == plain smart routing: no skill preference bias, standard memory injection. Used when a task type has no dedicated branch or its config is broken."
}
